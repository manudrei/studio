// tipi di numeri sono interi, float, double, binari, esadecimali
const numero  = '43'; // è una stringa
Number(numero) + 3;    //in questo caso è un numero cioè 46, perchè la funzione Number toglie le virgolette e tratta la stringa come un numero
//operatori matematici + - * / %(resto) **(potenza), resto =0 numeri è pari oppure se è diverso da 0 è dispari
// operatori di incremento ++ e operatore di decremento--
let numero1=10; //operatori di assegnamezione = += -= /= *=
numero1+= 5; //il risultato è 15

//operatori di comparazione < > <= >= == !=
// === comparatore identico, confronta anche il tipo di variabile
//!== comparatore identico di negazione